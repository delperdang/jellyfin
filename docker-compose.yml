version: '3.5'
services:
  jellyfin:
    image: jellyfin/jellyfin:latest
    container_name: jellyfin
    volumes:
      - /run/media/reguser/Elements/config:/config
      - /run/media/reguser/Elements/cache:/cache
      - /run/media/reguser/Elements/media/parents/movies:/media/parents/movies
      - /run/media/reguser/Elements/media/parents/shows:/media/parents/shows
      - /run/media/reguser/Elements/media/kids/movies:/media/kids/movies
      - /run/media/reguser/Elements/media/kids/shows:/media/kids/shows
    ports:
      - 8096:8096
    restart: 'unless-stopped'
    environment:
      - TZ=America/New_York